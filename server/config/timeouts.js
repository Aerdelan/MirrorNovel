// 生成流程超时常量。集中便于调优；接入时以实际代码值为准，避免行为漂移。
// 已接入：大纲、初始蓝图。其余调用点保留字面量，后续可按需迁移。

module.exports = {
  TIMEOUT: {
    // 60 分钟（每轮尝试各自享有）：大纲开启截断自动续写后，这是“每一轮”的预算；
    // 整本链路另有阶段死线（本值 × 3）作为多轮累加的外层保险。
    OUTLINE: 60 * 60 * 1000,
    BLUEPRINT: 60 * 60 * 1000, // 60 分钟（初始蓝图由推理模型生成，最慢）
    // 以下为参考值（尚未接入，与 routes/novel.js 中的字面量对应，便于后续统一）：
    CHAPTER: 15 * 60 * 1000,      // 900000  正文/续写/追加
    DESLOP: 10 * 60 * 1000,       // 600000  去AI味/润色/单章优化
    POLISH_REVISION: 10 * 60 * 1000,
    KEYWORDS: 5 * 60 * 1000,      // 300000  关键字/诊断/调优分析
    OPTIMIZE_ANALYSIS: 5 * 60 * 1000,
    DIAGNOSIS: 5 * 60 * 1000,
    EDITORIAL: 5 * 60 * 1000,     // 300000  编辑引擎单阶段
  },
};
