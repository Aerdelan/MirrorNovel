# 桌面端安装配置：把 npm 缓存与 Electron 二进制缓存都放到 E 盘
# 背景：C 盘空间紧张时，npm 临时目录与 electron 的 110MB 二进制会把 C 盘写满（ENOSPC），
#       导致安装中断且报错不明显。这里统一指到空间充足的盘。
cache=E:/npm-cache
electron_cache=E:/electron-cache
# 国内镜像（Electron 官方源在部分网络下不可达）
electron_mirror=https://npmmirror.com/mirrors/electron/
