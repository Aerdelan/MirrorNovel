# 桌面端安装配置
#
# 只放"任何机器都成立"的配置。踩过的坑：
# 曾经在这里写 cache=E:/npm-cache、electron_cache=E:/electron-cache（本机 C 盘紧张时的权宜配置），
# 结果 GitHub Actions 的 Windows runner 没有 E 盘，npm install 直接 ENOENT 失败（整个 CI 挂掉）。
#
# 本机想把 npm / electron 缓存挪到别的盘，请写进【用户级】配置（C:\Users\<你>\.npmrc），不要进仓库。

# Electron 二进制走国内镜像（官方源在部分网络下不可达）；
# CI 与打包脚本用同名环境变量覆盖，环境变量优先级更高。
electron_mirror=https://npmmirror.com/mirrors/electron/
